# ============================================================================
# AURA — Test Database Configuration Template
# Point this to a disposable test database or local test PostgreSQL instance
# ============================================================================

db.url=jdbc:postgresql://localhost:5432/aura_test_db
db.user=postgres
db.password=postgres
db.driver=org.postgresql.Driver
