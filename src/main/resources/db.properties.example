# Copy this file to db.properties (same folder) and fill in your local MySQL credentials.
# db.properties is git-ignored — never commit real credentials.

db.url=jdbc:mysql://localhost:3306/aura_db
db.user=root
db.password=changeme
